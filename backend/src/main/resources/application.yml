spring:
  application:
    name: onsinventory
  jackson:
    deserialization:
      # Jackson 3 default rejects requests that omit primitive DTO fields
      # (e.g. POST /api/inventory without "expired"); keep the Jackson 2 behavior
      fail-on-null-for-primitives: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:inventorydb
    driver-class-name: org.h2.Driver
    username: sa
    password:

server:
  port: 8080
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    nl.seanderoo.inventory: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG

app:
  security:
    enabled: false
  bootstrap:
    enabled: true
  jwt:
    secret: ${APP_JWT_SECRET:onsinventory-jwt-secret-key-minimum-32-characters-long}
    expiration: 604800000
  frontend-url: ${APP_FRONTEND_URL:http://localhost:5173}
  mail:
    from: ${APP_MAIL_FROM:noreply@onsinventory.com}
    feedback-to: ${APP_MAIL_FEEDBACK_TO:onsinventory@onsinventory.com}
  resend:
    api-key: ${RESEND_API_KEY:}
